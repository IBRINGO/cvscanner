export const environment = {
  production: true,
  // In production this is expected to be overridden at build time
  // (fileReplacements) or served from the same origin as the API gateway.
  apiBaseUrl: '/api/v1',
};
